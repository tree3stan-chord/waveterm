[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wave-visualizer"
version = "0.2.0"
description = "A terminal-based music visualizer inspired by web audio visualizers"
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Wave Team", email = "wave@example.com"}
]
maintainers = [
    {name = "Wave Team", email = "wave@example.com"}
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Sound/Audio :: Analysis",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Artistic Software",
]
keywords = ["music", "visualizer", "terminal", "ascii", "audio", "fft"]
requires-python = ">=3.8"

dependencies = [
    "numpy>=1.21.0",
    "rich>=12.0.0",
    "click>=8.0.0",
    "pydantic>=2.0.0",
    "toml>=0.10.0",
]

[project.optional-dependencies]
audio = [
    "sounddevice>=0.4.4",
    "librosa>=0.9.2",
    "pydub>=0.25.1",
]
export = [
    "pillow>=9.0.0",
    "imageio>=2.19.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=22.0.0",
    "isort>=5.10.0",
    "flake8>=4.0.0",
    "mypy>=0.991",
]
all = [
    "wave-visualizer[audio,export,dev]"
]

[project.urls]
Homepage = "https://github.com/wave-team/wave"
Documentation = "https://wave-visualizer.readthedocs.io"
Repository = "https://github.com/wave-team/wave"
"Bug Tracker" = "https://github.com/wave-team/wave/issues"

[project.scripts]
wave = "wave.cli:main"
wave-demo = "wave.cli:demo"

[tool.setuptools.packages.find]
where = ["."]
include = ["wave*"]
exclude = ["tests*", "docs*"]

[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"
multi_line_output = 3
line_length = 88

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
addopts = "--cov=wave --cov-report=html --cov-report=term-missing"